# Анализ: Instagram Export → Markdown для NotebookLM

**Статус:** реализация выполнена — скрипт `instagram_export_to_md.py`, см. README.

---

## 0. Инвентаризация экспорта в src/

В `src/` находится одна папка экспорта: **`instagram-br_hum4n-2026-02-04-DHYBumLm/`**.

### 0.1 Полный список JSON-файлов по разделам

| Раздел | Файлы |
|--------|--------|
| **personal_information** | `personal_information/personal_information.json`, `personal_information/instagram_profile_information.json`, `personal_information/note_interactions.json`, `personal_information/instagram_friend_map.json`, `personal_information/professional_information.json`; `device_information/devices.json`, `camera_information.json`; `information_about_you/profile_based_in.json`, `locations_of_interest.json` |
| **connections** | `followers_and_following/following.json`, `followers_1.json`, `close_friends.json`, `pending_follow_requests.json`, `recent_follow_requests.json`, `recently_unfollowed_profiles.json`; `contacts/synced_contacts.json` |
| **your_instagram_activity** | **messages:** `messages/inbox/*/message_*.json` (89 чатов, у одного — message_1..4), `messages/message_requests/*/message_1.json`, `messages/secret_conversations.json`; **comments:** `comments/post_comments_1.json`, `comments/hype.json`; **likes:** `liked_posts.json`, `liked_comments.json`; **media:** `media/posts_1.json`, `reels.json`, `stories.json`, `profile_photos.json`; **saved:** `saved_posts.json`, `saved_collections.json`, `saved_music.json`; **threads:** `threads/following.json`, `followers.json`, `personal_information.json`, `threads_and_replies.json`, `threads_viewed.json`, `liked_threads.json`; **story_interactions:** `polls.json`, `emoji_story_reactions.json`, `story_likes.json`; **subscriptions:** `show_exclusive_story_promo_setting.json`, `your_muted_story_teaser_creators.json`; **shopping:** `recently_viewed_items.json`; **avatars_store:** `avatar_items.json`; **monetization:** `eligibility.json` |
| **preferences** | `your_topics/recommended_topics.json`; `settings/comments_allowed_from.json`, `use_cross-app_messaging.json` |
| **logged_information** | `link_history/link_history.json`; `past_instagram_insights/*.json` (posts, live_videos, content_interactions, audience_insights, profiles_reached); `recent_searches/profile_searches.json`, `word_or_phrase_searches.json` |
| **ads_information** | `instagram_ads_and_businesses/*.json` (ads_about_meta, advertisers_using_your_activity_or_information, other_categories_used_to_reach_you, ad_preferences); `ads_and_topics/videos_watched.json`, `posts_viewed.json`, `ads_viewed.json`, `in-app_message.json` |
| **security_and_login_information** | `login_and_profile_creation/last_known_location.json`, `login_activity.json`, `logout_activity.json`, `signup_details.json` |
| **apps_and_websites_off_of_instagram** | `your_activity_off_meta_technologies.json` |

Медиа в экспорте: внутри `messages/inbox/*/` — папки `photos/`, `audio/` (голосовые .mp4), `videos/`; в корне — `media/` (other, posts, reels, stories). Для задачи в Markdown не используем.

### 0.2 Наблюдаемые типы сообщений в message_*.json

По просмотренным чатам (oleg_..., derzkijbew_..., и др.) в `messages[]` встречаются:

- **Текст:** `content` — обычное сообщение (Unicode, эмодзи).
- **Системные/служебные:** `content`: `"Liked a message"`, `"You sent an attachment."`, `"Reacted … to your message"` (без отдельного поля типа).
- **Шаринг:** объект `share`: `link`, `share_text`, `original_content_owner`; при этом `content` может быть `"You sent an attachment."` или текст.
- **Голосовые:** массив `audio_files[]`: `uri` (путь относительно корня экспорта), `creation_timestamp`; `content` при этом часто отсутствует.
- **Реакции:** массив `reactions[]`: `reaction`, `actor`.
- **Фото/видео:** в экспорте лежат в подпапках `photos/`, `videos/`; в JSON могут быть только системные подписи типа "You sent an attachment." / "… sent an attachment." без прямой привязки к файлу в том же объекте (в просмотренных примерах).

Имена участников: `participants[].name`, в сообщениях — `sender_name`. Кодировка — Unicode (при `json.load()` получаем нормальные строки).

### 0.3 Другие структуры, релевантные для разделов

- **close_friends.json:** `relationships_close_friends[]` → `string_list_data[]` с `href`, `value` (username), `timestamp`.
- **saved_posts.json:** `saved_saved_media[]` → `title` (автор), `string_map_data["Saved on"]`: `href`, `timestamp`.
- **secret_conversations.json:** `ig_secret_conversations` → `armadillo_devices[]`, `calls[]`; **содержимого переписок нет** (E2E), только метаданные устройств и пустой массив звонков.
- **threads_and_replies.json:** `text_post_app_text_posts[]` → посты Threads (медиа с `uri`, `title`, `text_app_post` и т.д.) — можно вынести в «Активность» как список постов с заголовками/ссылками, без медиа-файлов.
- **comments/hype.json:** `comments_story_comments[]` — та же схема, что и в post_comments: `string_map_data` с Comment, Media Owner, Time (timestamp).

---

## 1. Исходные данные (сводная структура)

### Структура экспорта Instagram (ваш экспорт)

Корневая папка: `instagram-br_hum4n-2026-02-04-DHYBumLm/`

| Раздел экспорта | Путь | Содержимое |
|-----------------|------|------------|
| **Профиль** | `personal_information/personal_information/personal_information.json` | `profile_user[]`: Name, Username, Email, Phone, Date of birth, Gender, Private Account |
| **Профиль (инсайты)** | `personal_information/personal_information/instagram_profile_information.json` | `profile_account_insights[]`: Contact Syncing, Last Login, First/Last Story Time и т.д. (часть — метаданные, не текст) |
| **Подписки / подписчики** | `connections/followers_and_following/following.json` | `relationships_following[]`: title (username), href, timestamp |
| | `connections/followers_and_following/followers_1.json` | Аналогичная структура |
| **Переписки** | `your_instagram_activity/messages/inbox/{conversation}/` | Папка на диалог. Внутри: `message_1.json`, при большом чате — `message_2.json`, `message_3.json` … |
| **Сообщения (формат)** | В каждом `message_*.json` | `participants`: список `{ "name": "..." }`. `messages`: массив сообщений |
| **Одно сообщение** | Элемент `messages[]` | `sender_name`, `timestamp_ms`, `content` (текст), опционально: `share`, `reactions`, `audio_files[]` (uri, creation_timestamp), `photos`, `videos` |
| **Комментарии** | `your_instagram_activity/comments/post_comments_1.json` | Массив объектов с `string_map_data`: Comment (value), Media Owner (value), Time (timestamp) |
| **Активность** | `your_instagram_activity/likes/liked_posts.json` | Кто и когда лайкнул (автор поста, ссылка, timestamp) |
| | `your_instagram_activity/likes/liked_comments.json` | Аналогично для комментариев |
| **Повторяющиеся темы** | `preferences/your_topics/recommended_topics.json` | `topics_your_topics[]` с `string_map_data.Name.value` (названия тем) |

Медиа и бинарные файлы: в экспорте есть папки `photos/`, `audio/`, `videos/` внутри чатов, а также `media/` в корне. Их не включаем в Markdown; для голосовых — только транскрипт в текст.

---

## 2. Соответствие требованиям

| Требование | Реализация |
|------------|------------|
| Извлечь только текстовые и аналитически полезные данные | Читаем только JSON; профиль, подписки, сообщения (в т.ч. текст из аудио), комментарии, лайки, темы. Медиа-файлы не читаем и не прикрепляем. |
| Игнорировать медиа и бинарные файлы | Не сканируем .jpg/.mp4/.webp и т.д. для контента; только ссылки на аудио для транскрипции (путь к файлу из `audio_files[].uri`). |
| Один .md с разделами: Профиль, Социальные связи, Переписки, Активность, Повторяющиеся темы | Один выходной файл, заголовки `## Профиль`, `## Социальные связи`, `## Переписки`, `## Активность`, `## Повторяющиеся темы`. |
| Каждый собеседник — отдельный чат | В разделе «Переписки» каждая папка из `inbox/` = один подраздел (один чат с одним/несколькими участниками по данным `participants`). |
| Аудио сообщения → текст и подставить в лог | Для сообщений с `audio_files` вызывать транскрипцию (например Whisper), результат подставлять в лог как текст сообщения от `sender_name` с той же датой. |
| Для сообщений: дата, собеседник, текст | Формат строки в MD: дата (из `timestamp_ms`), отправитель (`sender_name`), текст (`content` или результат транскрипции). |
| Сгруппировать переписки по пользователям | Уже так: одна папка = один диалог; внутри — все сообщения по времени (объединить message_1, message_2, … по timestamp). |
| Код чистый, читаемый, легко модифицируемый | Отдельный скрипт под Instagram (например `instagram_export_to_md.py`), функции по шагам: загрузка профиля, связей, инбокса, комментариев, активности, тем, сборка MD. Конфиг/CLI для путей и опций (транскрипция вкл/выкл). |
| Результат: .md, готовый к загрузке в NotebookLM | Один файл в `dist/` (или указанная папка), без бинарных вложений, в кодировке UTF-8. |

---

## 3. Детали реализации

### 3.1 Определение папки экспорта Instagram

- Искать в `src/` (или переданном пути) папку, в которой есть:
  - `personal_information/personal_information/personal_information.json`
  - `your_instagram_activity/messages/inbox/`
- Текущий `export_to_md.py` ориентирован на Telegram (`result.json` / `messages.html`), поэтому для Instagram нужен отдельный скрипт или явное ветвление по типу экспорта.

### 3.2 Профиль

- Читать `personal_information.json` → блок «Профиль»: имя, username, email, телефон, дата рождения, пол (если нужно для анализа).
- Опционально: выборочно добавить из `instagram_profile_information.json` (например Last Login как текст) — без лишних технических полей.

### 3.3 Социальные связи

- `following.json` → список подписок (username, дата по timestamp).
- `followers_1.json` (и при наличии followers_2, …) → подписчики.
- При желании: close_friends, pending_follow_requests — отдельными подпунктами в разделе.

### 3.4 Переписки

- Обход всех подпапок `inbox/`.
- Для каждой подпапки: собрать все `message_1.json`, `message_2.json`, … (сортировка по номеру файла), объединить массивы `messages`, отсортировать по `timestamp_ms`.
- Имя чата: из имени папки (например `oleg_1637392384330258`) или из `participants` (имена через запятую). Удобно: «Чат с Олег» / «Чат: Олег, User2».
- Для каждого сообщения:
  - Дата: `datetime.utcfromtimestamp(timestamp_ms / 1000).strftime(...)`.
  - Собеседник: `sender_name`.
  - Текст:
    - Если есть `content` — использовать его (экранировать для Markdown при необходимости).
    - Если есть `audio_files` — по пути из `uri` (относительно корня экспорта) вызвать транскрипцию; результат подставить как текст; при ошибке — подставить плейсхолдер типа `[Голосовое сообщение: не удалось распознать]`.
  - Системные/служебные (например "Liked a message") можно оставить как есть или кратко помечать `[Лайк сообщения]`.
- Share/link: при наличии `share.link` можно добавить строкой «Поделился ссылкой: …» после текста.
- Реакции: при необходимости — одной строкой после сообщения (кто какую поставил).

### 3.5 Аудио: конвертация в текст

- В экспорте Instagram голосовые идут как `.mp4` в папке `audio/` внутри папки чата.
- В JSON путь в `audio_files[].uri` задаётся относительно корня экспорта, например: `your_instagram_activity/messages/inbox/oleg_1637392384330258/audio/742431358493143.mp4`.
- Реализация: как в текущем `export_to_md.py` — Whisper (или опция без транскрипции). Путь к файлу: `os.path.join(export_root, uri)` (учёт разницы имён папки в JSON и на диске, если экспорт распакован в другое место).
- Итог: в лог чата подставляется только текст; сами файлы в .md не вставляются.

### 3.6 Комментарии и активность

- Комментарии: обход `comments/post_comments_1.json` (и при наличии других post_comments_*.json) → в раздел «Активность» подраздел «Комментарии»: дата, автор поста (Media Owner), текст комментария (Comment).
- Лайки постов/комментариев: из liked_posts.json / liked_comments.json — список с датой и автором/ссылкой (текстово), без медиа.

### 3.7 Повторяющиеся темы

- `recommended_topics.json` → `topics_your_topics` → из каждого элемента `string_map_data["Name"]["value"]` собрать список тем и вывести в раздел «Повторяющиеся темы».

### 3.8 Кодировка и эмодзи

- В JSON имена и текст приходят в Unicode (в т.ч. escape-последовательности типа `\u00d0\u009e`). При чтении через `json.load()` получим нормальные символы. Сохранять .md в UTF-8.

---

## 4. Структура выходного Markdown (пример)

```markdown
# Instagram Export — br_hum4n

## Профиль
- Имя: Maks Human
- Username: br_hum4n
- Email: brhumaan@gmail.com
- Телефон: +380632922587
- Дата рождения: 1990-01-14
- Пол: male

## Социальные связи
### Подписки (following)
- mikasa_superstar
- tattoo.vladimir.odesskiy
- bornto.co
...
### Подписчики (followers)
...
### Близкие друзья
...

## Переписки

### Чат с Олег
- 2025-09-04 12:35 | Олег | Привет, в общем всё-таки не завтра ...
- 2025-09-04 12:36 | Maks Human | [текст голосового: ...]
- 2025-09-04 12:38 | Олег | супер
...

### Чат с Stanislava
...

## Активность
### Мои комментарии
- 2025-12-20 | eupho_oriaa | 💝❤️♥️
- 2025-12-02 | andrey__akimov | Happy birthday bro ...
...
### Лайки постов
- eduard.terre — 2025-12-28 (ссылка на reel)
...

## Повторяющиеся темы
- Cars & Trucks by Make & Model
- Basketball
- Song & Music Creation
- Alligators & Crocodiles
...
```

---

## 5. Отличия от текущего кода (Telegram)

- Текущий скрипт `export_to_md.py` заточен под Telegram: `result.json` / `messages.html`, структура чатов и полей другая.
- Instagram: много маленьких JSON (по одному на чат + отдельные на профиль, подписки, комментарии, темы). Нет единого «result.json».
- Рекомендация: новый скрипт `instagram_export_to_md.py` (или `instagram_to_notebooklm.py`), который:
  - принимает путь к корню экспорта Instagram (или ищет его в `src/`);
  - последовательно собирает все разделы;
  - опционально использует общую логику транскрипции (Whisper) из существующего кода, чтобы не дублировать.

---

## 6. Риски и ограничения

- **Большие чаты**: объединение message_1..N и сортировка по времени — при очень большом числе сообщений может потребоваться потоковая обработка или лимит; для начала достаточно загружать все в память.
- **Аудио**: Whisper требует установки и времени; нужен флаг `--no-transcribe` для быстрого прогона с плейсхолдерами для голосовых.
- **Имена папок inbox**: формат `username_id` может содержать нестандартные символы; при выводе в MD лучше использовать `participants` для человекочитаемого названия чата.
- **NotebookLM**: лимит ~500k слов на источник — при очень большом экспорте может понадобиться разбиение на несколько .md (аналогично текущему `--max-words` в Telegram-скрипте).

---

## 7. Исследование (research): советы и референсы

### 7.1 Изменения формата экспорта Instagram/Meta

- **Раньше:** один файл `messages.json` со всеми диалогами (упоминается в старых туториалах и репозиториях).
- **Сейчас:** экспорт в виде **папки сообщений** — `messages → inbox → [папка на диалог]`, внутри каждой папки — `message_1.json`, при длинных чатах — `message_2.json`, `message_3.json` и т.д., плюс подпапки с медиа (photos, audio, videos). Формат близок к экспорту **Facebook Messenger** (Meta).
- **E2E-переписки:** отдельный экспорт (Download your data from end-to-end encrypted messages). В обычном экспорте в `secret_conversations.json` приходят только метаданные устройств и звонков, **без текста сообщений**.
- **Источники:** Stack Overflow (messages.json missing in Instagram download), справка Instagram/Facebook Help (Accounts Center, Data Download), инженерный блог Meta (data logs, 2024).

### 7.2 Готовые инструменты и примеры кода

| Ресурс | Описание | Применимость |
|--------|----------|--------------|
| **InstaParse** (jmsgnnr) | Парсит JSON из `messages → inbox → @username`, создаёт HTML с историей чата. Pandas, Jinja2. Работает с одним чатом (message_1.json в корне репо). | Идея: обход inbox по папкам, объединение message_1..N, сортировка по времени; вывод в читаемый формат (у нас — MD). |
| **insta-data-styled** (Adoliin) | Форматирует «messages.json» в веб-формат: папки `text-format` (по одному .txt на пользователя) и `web-format` (HTML). Один общий messages.json — устаревший формат. | Полезно: идея разбиения по пользователям и «читаемый текст»; у нас формат другой (inbox с подпапками), но подход переносим. |
| **InstagramJsonMessageReader** (vijaysattigeri) | Читает messages.json и выводит в читаемый вид. | Референс по парсингу participants/messages. |
| **instagram-messages-json-to-txt** (LorenKociko) | Извлекает текст из messages.json в .txt по пользователям. | Аналогично: один файл на чат, только текст. |
| **InstagramJSON2HTML** (D-Kats) | Конвертирует messages.json в HTML-отчёт с таблицами (отправитель, время). | Референс по структуре вывода (дата, автор, текст). |
| **Gemini_Json2md4NotebookLM** (minipoisson) | Конвертирует JSON (история Gemini из Google Takeout) в последовательные .md для загрузки в NotebookLM. | Паттерн: JSON → Markdown, оптимизированный под NotebookLM; адаптировать под структуру Instagram. |

**Вывод:** готового «Instagram JSON → Markdown для NotebookLM» нет; нужно адаптировать подходы: обход inbox по папкам, объединение message_*.json, извлечение текста (и опционально транскрипция аудио), сбор одного .md с разделами.

### 7.3 Рекомендации по формату для NotebookLM

- **Форматы источников:** NotebookLM принимает в т.ч. **Markdown**, PDF, текст; загружаемые файлы — до **200 MB** или **~500 000 слов** на источник.
- **Практики:** лучше загружать более **узкие и структурированные** документы; при очень большом экспорте — разбивать на несколько .md (например по разделам или по объёму слов). Явная структура с заголовками (##, ###) помогает навигации и ответам.
- **Источники:** справка Google (Add or discover sources), notebooklm.in (NotebookLM Sources And Best Practices), Medium (NotebookLM Hacks, document surgery).

### 7.4 Дополнительные замечания по реализации

- **Путь к экспорту:** в JSON `audio_files[].uri` задаётся **относительно корня экспорта** (например `your_instagram_activity/messages/inbox/.../audio/123.mp4`). При распаковке в другую папку нужен единый `export_root` для разрешения путей.
- **Share в сообщениях:** при наличии `share` полезно выводить в текст: ссылку + при необходимости `share_text` и/или `original_content_owner`, чтобы в Markdown сохранялся аналитически полезный контент без медиа.
- **Threads:** посты из `threads_and_replies.json` можно включить в раздел «Активность» как список с датой, заголовком (`title`) и ссылкой на медиа (если есть), без прикрепления самих файлов.

---

## 8. Итог

- Задача однозначно выполнима на основе текущей структуры экспорта Instagram.
- Нужен отдельный скрипт под Instagram, читающий перечисленные JSON и собирающий один .md с разделами Профиль, Социальные связи, Переписки (по пользователям, с датой/собеседником/текстом и подстановкой текста аудио), Активность, Повторяющиеся темы.
- Медиа и бинарные файлы не включаются; голосовые учитываются только как распознанный текст в логе чата.
- Код можно сделать модульным (функции под каждый раздел и сборку MD), с конфигом/CLI для путей и транскрипции.

Реализация: скрипт **instagram_export_to_md.py** генерирует один .md с разделами Профиль, Социальные связи, Переписки, Активность, Повторяющиеся темы; опция `--no-transcribe` для отключения транскрипции голосовых.
